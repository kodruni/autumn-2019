<?php

var_dump($_GET);

var_dump($_POST);

// if ($_GET['orderby'] == 'name') {
//     // use a functiion that orders by name
// }

// if ($_GET['orderby'] == 'rating') {
//     // use a functiion that orders by rating
// }

// select from database exactly $_GET['limit'] movies